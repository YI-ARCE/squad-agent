package log

var PlayerInfos = make(map[string]*PlayerInfo)

var JoinRequest = make(map[string]*PlayerInfo)

var BaseControllers = make(map[string]*PlayerInfo)

var NickNameInfo = make(map[string]*PlayerInfo)

var Disconnect = make(map[string]*PlayerInfo)
